`vsim -do scripts/rtl.do` to compile the RTL and auto run scripts/simulate.do 
`vsim -do scripts/gate.do` to compile the gate level netlist and run scripts/simulate.do
`dc_shell-xg-t -f scripts/synthesis.do` to compile the RTL into a gate level netlist